server:
  port: 8090
spring:
  application:
    name: Greensphere - Admin Portal Service
  datasource:
    url: jdbc:mysql://localhost:3306/greensphere?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

